# 환경/시크릿 (절대 커밋 금지)
.env
.env.local
*.pem
google-key.json

# 가상환경
.venv/
venv/
env/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# 접속 상세 로그 (main.py AccessDetailLogMiddleware)
logs/

# 숏크루 V2 로컬 DB
database.db

# 구버전 잔여 데이터 폴더(정리 전 임시)
data/
sample/

# AP-Framework 서비스 키 (실제값) — template 은 커밋
.AP-key.md

# n8n 실제 워크플로우 export (자동화 작업용 보관 — 시크릿 포함 가능, 추적 제외)
n8n-sample/
n8n-production/
# n8n/ 폴더는 README.md만 추적하고, 서버에서 당겨온 워크플로우 JSON(실제 크리덴셜 포함)은 커밋 제외
n8n/*.json
# 서버에서 내려받은 n8n 라이브 export(시크릿 포함 가능) — 추적 제외
n8n/live-export/

# 로컬 백업(시크릿 포함 가능) — 커밋 금지
.env.bak*
*.bak.[0-9]*
