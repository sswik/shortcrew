# 13. 유튜브 영상 발행 + 연결 인스타 자동 업로드 (구조 설계)

> 상태: **구조 세팅(스캐폴드)**. 실제 배선·최종 n8n JSON 반영은 운영자 확인 후 진행.
> Salog(`n8n-sample/Salog/10-새록-쇼츠발행파이프라인.json`)의 S5 단계를 **참고만** 했으며 코드는 섞지 않았다.

## 목표
렌더 완료된 쇼츠 영상 1개를 **① YouTube 업로드 + ② 연결된 인스타 릴스 자동 업로드** 한다.

## 역할 분담 (핵심)
| 단계 | 담당 | 근거 |
|---|---|---|
| YouTube 업로드 | **n8n `youTube`(OAuth2) 노드** | 채널별 OAuth 크리덴셜은 n8n 이 보유. 서버는 토큰 없음 → 서버 구현 안 함 |
| 영상 공개 URL 확보 | n8n(Drive 업로드+공개권한) 또는 기존 영상 공개직링크 | IG 컨테이너는 공개 URL 필요 |
| **인스타 릴스 발행** | **shortcrew `POST /admin/api/ops/instagram/publish-reel`** (이미 구현) | 컨테이너 생성→인코딩 폴링→발행까지 1콜 |
| 결과 ingest(youtube_id/ig_media_id 시트기록) | n8n → 기존 시트/OPS | 선택 |

## 흐름 (Salog S5 대응 — 참고)
```
렌더영상(공개 직링크)
  ├─ [n8n youTube 노드] YouTube 업로드 → youtube_id
  └─ [n8n httpRequest] shortcrew /instagram/publish-reel
         body: { channel_id, video_url(공개직링크), caption, share_to_feed:true }
         → 서버가 IG 컨테이너 생성·인코딩 폴링·발행 → { media_id, permalink }
  └─ [n8n] 시트에 youtube_id·ig_media_id 기록(선택)
```

## shortcrew 측 (이미 존재 — 추가 구현 불필요)
- `app/admin/ops/routes/instagram_publish.py` → `POST /admin/api/ops/instagram/publish-reel`
  - 인증: `X-Ops-Token: $OPS_API_TOKEN`
  - IG 계정/토큰: `CHANNEL_{id}_IG_ACCOUNT_ID` / `CHANNEL_{id}_IG_ACCESS_TOKEN` (00식 id: 02~37)
  - `dry_run:true` 로 컨테이너 payload 미리보기 가능
- 스텁 경계 명시: `app/admin/ops/services/youtube_ig_bridge.py`

## n8n 자리 (placeholder)
- 템플릿: `n8n/WF-유튜브발행-인스타크로스포스트.template.json`
  - 노드 뼈대: 트리거 → 영상행 조회 → **[TODO] youTube 업로드(OAuth 크리덴셜 채널별)** → (Drive 공개) → **shortcrew /instagram/publish-reel 호출** → 결과 시트기록.
  - 운영자가 채널별 youTube 크리덴셜·시트 ID·`X-Ops-Token` 을 채워 현재 연결된 n8n 에 반영.

## 확인/배선 체크리스트 (기상 후)
- [ ] 채널별 유튜브 OAuth 크리덴셜을 n8n 에 등록
- [ ] 영상 공개 직링크(Drive 공개 URL) 확보 방식 결정
- [ ] `/instagram/publish-reel` 을 `dry_run:true` 로 채널 1개 검증
- [ ] youtube_id·ig_media_id 시트 컬럼/기록 위치 결정
- [ ] 템플릿 워크플로우를 현재 n8n(n8n.rodimap100.com)에 반영

## 최종 아키텍처 (확정)
- **당일 신규**: 각 채널 **기존 영상생성 워크플로우 S5(발행)단에 IG 노드 추가**(Drive공개→`/crosspost/reel`). 별도 WF 아님. 참고 노드: `n8n/REF-당일신규_기존WF에추가할_IG노드.template.json`.
- **레거시 백필**: **마스터 1개**(`n8n/WF-과거영상-인스타백필-MASTER.template.json`) — 13펌프채널 디스패처+순차처리(안 섞임), 채널당 하루 3개(09/13/17시), Wait로 버스트 방지. ig_media_id 로 완료 마킹.
- 인플루언서 02/03/04 는 기존 `ig_url` 크로스포스트 유지(별도).

## 반영완료 (2026-07-23)
당일 신규 IG 크로스포스트를 **13펌프채널에 실제 반영**하고 실발행까지 검증 완료.

- **대상 13채널(=00식 id, WF번호와 동일)**: 05·09·10·11·13·14·15·23·24·25·33·34·37 (env `CHANNEL_{id}_IG_*` 세팅된 16채널 중 인플루언서 02/03/04 제외).
- **삽입 체인**: 각 채널 `YouTube 업로드` 노드 뒤 병렬 브랜치로 `IG 공개권한(anyone)` → `IG 발행준비(공개URL+캡션)` → `IG 릴스 발행(/crosspost/reel)`. 기존 발행흐름 보존. 공개URL=`drive.google.com/uc?export=download&id=`, 캡션=제작단계 title+description. `channel_id`만 전달(IG토큰은 서버가 `CHANNEL_{id}_IG_*`로 처리). WF05(홈카메라 v6)만 템플릿이 달라 `Stage2 시작`의 `drive_video_id` 소스로 개별 배선.
- **토큰 인증**: `$env` 대신 n8n Header Auth 크리덴셜 `shortcrew OPS (X-Ops-Token)`(id `QPyvSGRgwx6QACob`) 생성해 노드에 연결 → env/호스트 접근 불필요, JSON에 평문 미노출.
- **안전장치**: IG 발행·Discord 노드에 `onError: continueRegularOutput` → 발행/알림 실패해도 쇼츠 발행 플로우 중단 안 됨.
- **부수 수정**: `Discord 알림 (완료)` 노드가 Switch(`단계 라우터`)의 빈 출력0을 참조해 throw("JSON parameter needs to be valid JSON")하던 기존 버그를 단일출력 `대상 행 필터` 참조로 교정(9채널; WF13/14/15는 원래 정상).
- **실발행 검증**: 채널33 No=93 영상으로 `/crosspost/reel` 실호출 → 릴스 발행 성공(`media_id` 반환, reel `https://www.instagram.com/reel/DbI14WAiARx/`). 시트 `ig_media_id` 기록.
- **미검증(저위험)**: n8n 크리덴셜 경로(n8n이 엔드포인트 호출 시 헤더 주입)는 다음 정규 발행(상태=영상완료 행이 업로드단 통과) 때 자동 검증.
- 라이브 동기화본은 `n8n/live-export/`(gitignore — 인라인 웹훅/키 포함하여 커밋 제외).
