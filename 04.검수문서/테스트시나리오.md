# 테스트 시나리오

> shortcrew 검증 시나리오. (근거: `tests/test_smoke.py`, `tests/test_shorts_sheet_matcher.py`)
> 실행: `python3 -m unittest tests.test_smoke -v` / `tests.test_shorts_sheet_matcher -v`

## 1. 스모크 (`test_smoke.py`)

| TC-ID | 시나리오 | 기대 |
|-------|----------|------|
| TC-S01 | `GET /health` | 200 |
| TC-S02 | `www` 호스트 | apex 리다이렉트 |
| TC-S03 | `/about` 페이지 | 정상 노출 |
| TC-S04 | OpenAPI ops channels 포함 | 스펙 노출 |
| TC-S05 | `GET /admin/login` | 로그인 화면 |
| TC-S06 | ops channels 인증 동작 | 미설정=200 / 설정=302 |
| TC-S07 | `/admin/reviews/new` 인증 | 미인증 보호 |
| TC-S08 | 채널 env 키 `CHANNEL_{id}` 패턴 | 일치 |
| TC-S09 | 기본 채널 id = 201 | 일치 |
| TC-S10 | 구글 시트 누락 응답 | 누락 env 키 안내 |

## 2. 쇼츠 시트 매칭 (`test_shorts_sheet_matcher.py`) ★핵심 추가

| TC-ID | 시나리오 | 기대 |
|-------|----------|------|
| TC-M01 | youtube `watch?v=` URL 정규화 | 영상 키 추출 |
| TC-M02 | `/shorts/` URL 정규화 | 영상 키 추출 |
| TC-M03 | `youtu.be` 단축 URL | 영상 키 추출 |
| TC-M04 | 공백·등가 표기 정규화 | 동일 키 |
| TC-M05 | strict 키 매칭 | 정확 매칭 |
| TC-M06 | 단일 행 조인 | 계획×상품 결합 |
| TC-M07 | 상품 매칭 없을 때 스킵 | 발행 안 함 |
| TC-M08 | 발행일이 기준일과 다르면 스킵 | 발행 안 함 |
| TC-M09 | 날짜 ISO 문자열 파싱 | 정상 |
| TC-M10 | 시트 serial 날짜 파싱 | 정상 |

## 수동 검증 (브라우저)

- 백오피스 메뉴(상품·리뷰·인플루언서·시트·DM·로그) 동작.
- 리뷰 작성(Toast UI Editor) → 발행 → 공개 `/{slug}/review/{id}` 노출.
- AI 리뷰 초안(`/ai/review-draft`) 생성.
- 쇼츠 자동화 수동 실행: `POST /admin/api/ops/shorts-review/run` → `reviews` 생성·시트 상태 갱신.
- 공개 몰 상품 클릭 → 쿠팡 이동 + 클릭 로그 적재.
